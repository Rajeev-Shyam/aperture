{
  "name": "aperture-ui",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Aperture WebView overlay UI — Chromemorphism bubbles, context-preview trust surface, voice surfaces (doc 11 + doc 14). Talks to the Rust core over the Tauri IPC contract only.",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "@tauri-apps/api": "^2.1.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^5.4.11",
    "typescript": "^5.6.3",
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1"
  }
}
