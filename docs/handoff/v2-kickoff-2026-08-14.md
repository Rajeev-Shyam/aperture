<!-- v2 preparation doc (process, not architecture). Bridges the SHIPPED v1
     (session-bridge-2026-08-14-appification.md) to the Doc 22 v2 draft.
     Doc 22 remains the spec; this maps its assumptions onto what NOW exists
     and sequences the pre-build work. -->

# 🚀 v2 Kickoff — Agent Execution Layer, grounded in shipped v1

**Prepared: 2026-08-14, at v1 completion.** Read AFTER
`session-bridge-2026-08-14-appification.md` (v1 state) and WITH Doc 22 (the
v2 spec draft). Doc 22's rule stands: **grill Q-V2-01…Q-V2-10 before writing
any v2 crate.** This doc makes that grilling cheaper by marking what v1's
final form already answers.

---

## 1. v2 assumptions that shipped v1 has OVERTAKEN (update Doc 22 first)

| Doc 22 says | Reality as of 2026-08-14 |
|---|---|
| MCP server "deferred" (§4.1 builds on a future gateway extension) | **The MCP stdio server is LIVE**: `aperture-mcp` (gateway crate) ↔ named pipe ↔ `src-tauri/mcp_bridge.rs`, registered in `claude_desktop_config.json`, four tools, per-query approval gate, audited releases. `agent_step` should be a **fifth MCP tool** (or a pipe op) on THIS plumbing, not a new transport. |
| Transparency gate to be designed for agent mode (§4.1) | The mechanism exists and is battle-tested: `PreviewStore` sessions + **content-bound approval** (SHA-256 at approve, re-checked at release) + `preview_request` event → panel. Scoped-allow-per-task = a persisted approval that survives N releases — an extension of `previews.approved`, not new machinery. |
| VLM optional/"v2.1 question" (Q-V2-05) | Qwen2.5-VL-3B runs locally NOW: ~1 s/frame on the RTX 5060, perfect structured scene JSON, 4.3 GB VRAM, <3 s release on kill. Q-V2-05 can be answered EMPIRICALLY (compare Claude-vision-only vs +VLM-scene on the same task set). |
| Hard stop "separate lightweight process, IPC [OPEN M-V2-0]" | The **system tray** now exists (always visible, its own menu thread) and the named-pipe pattern is proven twice (NM host, MCP). A tray "Stop task" + a dedicated `aperture-stop` pipe is the obvious M-V2-0 shape — decide whether tray-in-process is acceptable or the doc's separate-process stance stands. |
| Screenshot pipeline to build (§3.2) | v1 already downscales + JPEGs frames for the VLM (`vision-ocr` ≤1024 px q85) and the `ContextPayload` schema already carries a `screenshot` item. The serializer mostly composes existing parts. |
| Exclusion check in executor "[OPEN M-V2-3]" | Exclusions are hot-reloadable + shared-handle (`ExclusionList`); the MCP search already demonstrates the read path (`redaction_flags = 0`). The executor consumes the same handle. |

## 2. Q-V2 grilling — what tonight's data already says

- **Q-V2-02 (payload size):** measurable TODAY against the live MCP chain —
  base64 a 768 px screenshot into a `ContextPayload`, stage it, have Claude
  Desktop fetch via `aperture_get_context`. No design work needed, one test.
- **Q-V2-05 (VLM in the loop):** default NO (Claude sees the screenshot), but
  the local VLM is cheap enough (~1 s) that "VLM pre-digest when the payload
  budget is tight" is a live option; test empirically at V2-M2.
- **Q-V2-10 (VRAM tier):** the default agent loop is 0-VRAM (doc §7) — the
  only contention is optional agent-VLM; slotting `agent-VLM = 75` into the
  existing `priority` table is a one-line decision. Recommend accepting the
  ASSUMPTION and moving on.
- **Q-V2-06 (context growth):** the MCP pull model means Claude Desktop holds
  the conversation; prior-steps-summary lives in ITS context, not ours.
  Re-examine the question in that light — it may mostly dissolve.
- Still genuinely open and blocking: **Q-V2-01 (UIA reliability)** — needs the
  V2-M0 spike; **Q-V2-03/09 (excluded apps / UAC)** — product decisions;
  **Q-V2-07 (error recovery)**; **Q-V2-04 (step cap)**; **Q-V2-08 (local
  planner — recommend OUT, per locked decision 1)**.

## 3. v1 leftovers that become v2 pre-work (do these first)

1. **Gate harness bodies** (`sc6_vram_release.rs` todo!(), SC3/SC4/SC5-byte,
   PresentMon): the agent loop leans harder on toggle/VRAM invariants — the
   harnesses should be real before an executor exists. Live figures to encode:
   VLM 4.7 GB→439 MB <3 s; STT 0.93 conf/0.8 s; whisper+Qwen cold loads.
2. **MCP submit→bubble rendering** (validated suggestions currently only
   acknowledged) — v2's "task complete bubble" (§9.4) wants this path anyway.
3. **Data-dir/install-dir separation** (both are `%LOCALAPPDATA%\Aperture`;
   verified safe today, but v2 adds `tasks`/`task_steps` tables — worth doing
   the `%APPDATA%` migration before the schema grows).
4. **VLM weights distribution** for non-dev machines (installer can't carry
   3 GB): decide fetch-on-first-use (fastembed-style) vs documented manual.
5. **GPU STT** ([VERIFY]) — optional; CPU base.en is adequate for PTT.

## 4. Suggested build order (maps to Doc 22 §10)

1. **Grill session** — resolve Q-V2-01..10 with Rajeev; update Doc 22 in
   place; promote it from Draft.
2. **V2-M0 spike**: `action-executor` UIA click/type/launch against a test
   app + the Q-V2-01 reliability report. No loop, no cloud.
3. **V2-M1**: `screen-serializer` composing existing capture/OCR/redaction;
   payload-size measurement closes Q-V2-02.
4. **V2-M2+** per the Doc 22 table, riding the live MCP gate for approvals.

## 5. Invariants that must survive v2 (verbatim from v1, non-negotiable)

7.0 GB VRAM ceiling · two-emitter egress rule (the executor is NOT an
emitter — it never touches the network) · capture toggle <3 s release ·
exclusions never fail open · every egress user-approved + audited · the
tray/hard-stop always works.
