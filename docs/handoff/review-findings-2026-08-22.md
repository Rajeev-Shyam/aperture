# Adversarial review findings — 2026-08-22 tree (v2 wiring session)

> Raw reviewer output from the multi-agent review of the tree described in `session-bridge-2026-08-22-v2-wiring.md`.
> ⚠️ The verify/skeptic phase mostly died on account session limits (54 of 71 agents errored), so these are the reviewers' **raw confirmed-by-reviewer** findings — severities are the reviewers' own, not all adversarially re-verified. Triage each against the current code before fixing; some may already be fixed (see the bridge §6 status list, which is authoritative for what is done).

---

[high] Hard stop pressed while an action is in flight is never applied to the driver (state stays Running, next screen still released) — src-tauri/src/agent.rs:576
   Task Running; Claude sends a `click`; mcp_step takes the driver out (in_flight=true) and spawn_blockings `execute`. The user presses Stop on the surface (or the tray item). `user_decide` (agent.rs:302-311) only stores `rt.stop=true` and returns the stale snapshot because `rt.driver` is None; `hard_stop_current` (793-803) likewise finds no driver and returns None (tray logs 'no task running'). UiaExecutor checks the flag only in `guard()` before dispatch (uia.rs:62-73, 298-299), so a click/key/sc
   FIX: After re-inserting the driver (agent.rs:575-579) and again at the top of `observe_and_release`, check `rt.stop.load(SeqCst) && !d.is_terminal()` and call `d.hard_stop(now)`; then route through `terminal_reply` instead of observing. Also make `user_decide("stop")`/`hard_stop_current` record that a st

[high] Paused tasks are not gated: a later aperture_agent_step call observes and executes while the task is Paused (Excluded/Elevated/Clarification) — src-tauri/src/agent.rs:454
   Action hits an excluded window → `execute` returns Err(Excluded) → driver transitions Running→Paused (driver.rs:411-413) → `wait_for_resume` times out after 110 s → notice tells Claude 'call again (no instruction) after they resume'. Claude calls again (with or without an instruction) before the user presses Resume. Phase A passes; `wait_for` only requires `is_approved() || is_terminal()` (454-459), which is already true; `instruction == None` falls to `to_execute = None` (551-553) and `observe_
   FIX: In `mcp_step`, after the approval wait, require `d.state() == TaskState::Running && d.pause().is_none()` (wait for it with the same bounded `wait_for`, returning the pause notice on timeout) before either classifying or observing. Defensively, make `classify` return a `Disposition::Paused(reason)` w

[medium] Stale confirmation chip: classify ignores a pending action, so a later Approve executes an action from an old screen — crates/agent-loop/src/driver.rs:264
   Claude sends `click "Delete"` → `classify` sets `pending`/`pause=Confirm` (driver.rs:314-316) but the machine stays Running. The 110 s wait expires; Claude ignores 'same instruction' and calls with no instruction: agent.rs:551-553 → observe_and_release releases a new screen with the chip still displayed. Claude now sends a harmless `click "Next"`: `classify` does not look at `self.pending`, returns `Execute`, the action runs, the screen changes, `pause` stays `Confirm{Delete}` and `pending` stay
   FIX: In `classify`, if `self.pending.is_some()` return `NeedsConfirm` for the existing pending (do not overwrite) or clear it explicitly when a different instruction arrives (audit the old one as skipped). In `mcp_step`, refuse to observe while `pause.is_some()` (see the Paused finding) and drop `approve

[medium] A Skip pressed after the 110 s confirm timeout is undone: Claude is told to resend the same instruction, which re-arms the chip — src-tauri/src/agent.rs:538
   Consequential action → chip shown → `wait_for` at 528-536 times out → notice 'Call again with the SAME instruction once they have'. The user presses Skip in the gap: `confirm(false)` writes a skipped row, sets `last_action = skipped by user`, increments the step, leaves `approved_action = None`. Claude obeys and resends the identical instruction: `approved_action.take()` is None, so 501-505 re-runs `classify`, which re-sets `pending`/`pause=Confirm` and re-shows the same 'Approve Delete?' chip t
   FIX: Keep the unresolved instruction in the runtime (it already lives in `driver.pending`) and tell Claude to call again with NO instruction; on the next call, if `pending` is Some, wait on it instead of re-classifying; if the user already skipped, fall through to observe so the `last_action: skipped by 

[medium] No-op and no-instruction turns bypass the step cap and break the step-row/payload-hash pairing — crates/agent-loop/src/driver.rs:303
   Claude repeatedly sends `{status: continue, action: {type: none}}` (or omits `action`, or omits `instruction` entirely; mcp_bridge.rs:222 also silently turns any non-object `instruction` into None with no error). `classify` returns `Noop` without `record_row`/`record_step`; agent.rs:549/551-553 then sleep and `observe_and_release`. `machine.step` never increments, so the Doc 22 §3.3 step cap (RK-V2-03 runaway guard) never fires: unbounded screenshot releases, each a cloud_send row but never a ta
   FIX: Treat `Noop` and the no-instruction observe (after the first) as a step: `record_row("none", …, Skipped)` + `record_step(false)` so the cap applies and the hash pairs; have `agent_step` return an `isError` notice for a non-object `instruction` instead of dropping it.

[medium] Stop during the observation phase still releases the screen; `observed()` lands on a terminal driver — src-tauri/src/agent.rs:747
   `observe_and_release` reads task text/step under the lock (658-667), then spends hundreds of ms to seconds in spawn_blocking (capture + OCR + image redaction + JPEG). The user presses Stop meanwhile: `user_decide` → `hard_stop` → row + Cancelled (the driver is in the mutex, so this succeeds). Back in `observe_and_release` nothing re-checks the driver: the cloud_send audit row is written, the redacted screenshot + OCR text are returned to Claude with 'Reply by calling aperture_agent_step again', 
   FIX: Right before `record_cloud_send` (and again before returning), lock the runtime and check `driver_matches(id) && !d.is_terminal() && !rt.stop.load()`; otherwise drop the observation and return `terminal_reply`.

[medium] Sequential bounded waits inside one agent_step call can exceed the 180 s pipe timeout, orphaning the handler (decision consumed, screen released to a dead pipe) — src-tauri/src/agent.rs:54
   `USER_WAIT` is 110 s but one call can run several waits back-to-back: approval (454) → Clarify wait (515), or NeedsConfirm (528) → execute → Excluded → `wait_for_resume` (586). Example: Claude-created task, Claude calls step with `need_clarification`; the user approves at 90 s, the call then waits up to 110 s more for the answer (200 s). `aperture-mcp` gives up at `CALL_TIMEOUT` 180 s (crates/reasoning-gateway/src/bin/aperture_mcp.rs:27) and Claude sees an error. The app-side handler keeps runni
   FIX: Compute one deadline per `mcp_step` call (e.g. 150 s) and pass the remaining budget into every `wait_for`; when the budget is gone return the notice instead of starting another wait. Consider a per-task `turn` guard (set during the whole mcp_step, not just execution) so a retry cannot overlap an orp

[medium] A panic on the blocking executor thread drops the driver and leaves `in_flight` true forever (runtime wedged until restart) — src-tauri/src/agent.rs:572
   `spawn_blocking` moves the driver into the closure; on a panic, `JoinError` is mapped to `Err` and returned, but `rt.in_flight` stays true and `rt.driver` stays None. A concrete panic site on that path: `record_row` → `TaskManager::record_step` → `Db::with_conn` uses `expect("db mutex poisoned")` (crates/db/src/lib.rs:221) — once any other thread panics while holding the DB mutex, every executor step panics. Afterwards every `mcp_step` returns 'another step is still executing', `user_decide(stop
   FIX: On `JoinError`, lock the runtime, set `in_flight=false`, mark the DB task Failed via `TaskManager::transition` and render a terminal snapshot; or run `execute` under `std::panic::catch_unwind(AssertUnwindSafe(..))` inside the closure and always return the driver.

[medium] Lost wakeup in `wait_for`: the Notified future is created after the predicate check, so a decision landing in the gap sleeps until the 110 s deadline — src-tauri/src/agent.rs:426
   `wait_for` evaluates `pred` under the mutex (419), drops the guard, then calls `state.agent_notify.notified()` (426). tokio's `Notify` only guarantees `notify_waiters()` wakeups for `Notified` futures that already exist (tokio 1.52.3 notify.rs docs). Timeline on the multi-thread runtime: mcp_step sees 'not approved' and releases the lock → `user_decide("approve")` takes the lock, approves, releases, calls `notify_waiters()` (333) → mcp_step only now creates `notified()`. The notification is lost
   FIX: Create and pin the `Notified` future BEFORE checking the predicate on every loop iteration (`let notified = state.agent_notify.notified(); tokio::pin!(notified); if pred(...) {return true;} timeout(.., &mut notified).await`), or switch to `tokio::sync::watch`/a generation counter checked under the l

[medium] MCP result cap for agent steps is checked after the audit row and under-counts the base64 wire size — src-tauri/src/mcp_bridge.rs:228
   `agent_step` compares `text.len() + raw_jpeg.len()` against `MCP_RESULT_MAX_BYTES`, but the wire carries the JPEG base64-encoded (4/3) inside a JSON envelope, so a reply of e.g. 600 KB text + 400 KB JPEG (1.00 MiB raw) passes and ~1.13 MiB leaves over stdout, contrary to `get_context` which caps `wire.len()` (309). Conversely when the cap does trip, `observe_and_release` has already written the `cloud_send` audit row (agent.rs:747) and called `d.observed(hash)` (763): the audit trail records a r
   FIX: Compute the wire size (text + base64 length + envelope) inside `observe_and_release` BEFORE `record_cloud_send`, returning the over-cap notice without auditing or calling `observed`; clamp `step_summary` length in `classify`.

[low] Task identity is not re-validated after waits; a superseded call observes or acts on the next task — src-tauri/src/agent.rs:617
   Only Phase A checks `driver_matches(id)` (450). `wait_for_resume` returns true on `d.task_id() != id` (617), `wait_for` approval returns true for whichever driver is current, and `terminal_reply`/`observe_and_release` read `rt.driver` without an id check (660). Scenario (needs the lost-wakeup window or a slow wake): task A is Paused and a step call is waiting; the user stops A, dismisses it and types task B (pre-approved). When the waiter wakes at the deadline, the predicate passes on B, `termin
   FIX: Make every post-wait check id-aware: after each `wait_for`, re-lock and require `driver_matches(&rt, id)`, else return the 'unknown or superseded task_id' error; have `observe_and_release` and the execute block verify the id before touching the driver.

[low] Stop with a stale task_id raises the CURRENT task's stop flag, then reports an error — src-tauri/src/agent.rs:305
   `user_decide` stores `rt.stop = true` before checking `d.task_id() != id` (313). A surface instance holding an old task id (e.g. a second monitor's overlay that missed the `agent_task` event while Claude created a new task via `mcp_start`) presses Stop: the new task's executor flag is raised, the UI gets 'that task is no longer current', and the new task is then cancelled at its next classify/execute (driver.rs:265, uia.rs:63) with a 'hard_stop' row the user never intended for it — while the cal
   FIX: Check `driver_matches(&rt, id)` (or `in_flight`) before storing the flag; only raise `rt.stop` when the id matches the current/in-flight task (keep the no-id tray path unconditional).

[low] `approve`/`resume` mutate the in-memory machine before the DB transition; a DB error leaves the task wedged with an IllegalTransition on retry — crates/agent-loop/src/driver.rs:230
   `approve`: `self.machine.start()?` moves the machine to Running, then `tasks.transition(..Running..)` fails (busy/locked encrypted DB, poisoned-mutex recovery, disk error) → `approved` stays false and `pause` stays `Approval`. The surface still shows the approval card; pressing Approve again hits `machine.start()` → `IllegalTransition { Running -> Running }` returned verbatim to the UI, and `mcp_step`'s approval wait (`is_approved()`) can never pass; only Stop clears it. `resume` (365-372) has t
   FIX: Perform the persistence transition first and only then mutate the in-memory machine (or roll the machine back on DB error), and make `approve`/`resume` idempotent when the machine is already in the target state.

[high] Agent loop observes and ACTS while the task is Paused — the Resume gate is advisory only — src-tauri/src/agent.rs:444
   `mcp_step` phase A only waits for `is_approved() || is_terminal()` (lines 444-461) and then, with no instruction, sleeps and calls `observe_and_release` (607-610); nothing checks `state() == Paused` / `pause().is_some()`. Every pause path that returns a 'call again after they resume' notice to Claude (observation-time `CaptureError::Excluded` → `pause_for`, line ~700; `wait_for_resume` timeout after `ActionError::Excluded/Elevated`, line ~590; `Clarify` wait timeout, line ~520) therefore relies 
   FIX: In `mcp_step`, after the approval wait and before phase B/C, gate on the driver being `Running` (or terminal): `wait_for(state, |rt| driver.state() != TaskState::Paused && pause().is_none() || terminal)`; on timeout return the same 'paused — waiting for the user' notice without observing. Additional

[medium] Stop pressed during an in-flight action is not acknowledged: one more screen is released and the bar keeps saying 'running' — src-tauri/src/agent.rs:576
   While the driver is on the blocking thread (`in_flight`), `user_decide('stop')` only sets the flag and returns the stale snapshot (line 308-310) — no transition, no event. When the action completes after its last `check_stop` (a `click`/`scroll`/`switch_window`/`launch` that already passed the guard returns `Ok`, uia.rs:298-321), the shell puts the driver back (576-578), sees `Ok(_)` and a non-terminal driver (599-605), sleeps, and calls `observe_and_release` — a redacted screenshot + OCR is aud
   FIX: After the blocking execute returns and `rt.driver = Some(driver)` (line 576), check `rt.stop.load(SeqCst)`; if set and the driver is not terminal, call `driver.hard_stop(now)`, emit the view, and return `terminal_reply` — never fall through to `observe_and_release`. Also have `user_decide('stop')` w

[medium] Clarification answer is typed into the user's foreground app, not the agent card — ui/src/components/AgentSurface.tsx:266
   The `clarification` PauseCard (248-282) renders an `<input autoFocus>` inside `.agent__bar`, which is only hover-interactive (hit-test rects) — no `useModalSurface`/`focusOverlay()` call like the composer and every other panel have. The overlay window is created `focus: false` (tauri.conf.json:23); `autoFocus` moves DOM focus but OS keyboard focus stays on whatever window is in front, which at this moment is the app Claude was just driving. Scenario: Claude asks 'Which account should I use?' → t
   FIX: Give the pause cards the same treatment as TaskComposer: a ref + `useModalSurface(ref, { exclusive: false })` (which calls `focusOverlay()` and moves focus in) on the `alertdialog` wrappers, at minimum for `clarification`; or call `focusOverlay()` from an effect when `view.pause` transitions to a no

[low] TaskComposer: useModalSurface steals focus from the autoFocus textarea — composer opens with nothing focused — ui/src/components/AgentSurface.tsx:350
   React applies `autoFocus` (line 383) during the layout phase of the mount commit; `useModalSurface`'s passive effect then runs `ref.current?.focus()` (useModalSurface.ts:65) on the wrapper `div tabIndex={-1}`, moving focus OFF the textarea. Scenario: user clicks ✦ and starts typing the task → keystrokes go to the focused wrapper div (no-op) until they click into the textarea or press Tab. Bubble.tsx avoids this because its autoFocus'd element is not under a `useModalSurface` root.
   FIX: In TaskComposer, after `useModalSurface`, focus the textarea explicitly in an effect (`useEffect(() => textareaRef.current?.focus(), [])`), or extend `useModalSurface` to skip `ref.current.focus()` when `ref.current.contains(document.activeElement)`.

[low] Agent bar is pinned to the same bottom-centre slot as the voice surfaces and a bottom-centre HUD, and stacks above both — ui/src/styles/agent.css:9
   `.agent` is `position: fixed; left: 50%; bottom: var(--edge-inset); transform: translateX(-50%)` with `z-index: 35` — identical anchor to `.voice` (bubble.css:456-459, no z-index → below 35) and to the HUD's `bottom-center` anchor (Hud.tsx:42, `.hud` z-index 30). Scenario A: a task is live (bar ~560 px wide, up to ~250 px tall with the log + a card); the user holds PTT → the listening pill, the 'Did you say…?' chip and its Run/Dismiss buttons, or the answer bubble render underneath the opaque `.
   FIX: Offset the agent stack (e.g. `bottom: calc(var(--edge-inset) + 72px)` or anchor it bottom-left of the bubble stack), or give `.voice` a higher z-index than `.agent` and have the HUD's bottom-center anchor shift up when `.agent` has content. At minimum lower `.agent` below `.hud` (z 30) so the HUD st

[low] AgentSurface `agent_task` listener can leak on a fast unmount / StrictMode remount — ui/src/components/AgentSurface.tsx:68
   The effect stores the unlisten fn only when the `listen()` promise resolves (`.then((u) => { unlisten = u; })`) and the cleanup reads `unlisten?.()` synchronously. If cleanup runs before the promise resolves — React.StrictMode's mount→unmount→mount in dev (main.tsx:19), or any future conditional mount — the first subscription is never removed: every `agent_task` event then invokes a dangling handler (two `setView` calls per event in dev, a handler on an unmounted component in prod). App.tsx:103-
   FIX: Mirror App.tsx: `let cancelled = false; void onAgentTask(setView).then((u) => { if (cancelled) u(); else unlisten = u; }); return () => { cancelled = true; unlisten?.(); };` and apply the same guard to the initial `agentStatus()` fetch so a late response does not overwrite a newer event.

[low] Agent step writes the `cloud_send` audit row BEFORE the MCP size-cap check can refuse the release — src-tauri/src/agent.rs:747
   `observe_and_release` records `cloud_send` (747-760) and marks the driver `observed(hash)` (763), then returns the `StepReply` to `mcp_bridge::agent_step`, which only afterwards checks `reply.text.len() + image_len > MCP_RESULT_MAX_BYTES` (mcp_bridge.rs:228) and replies 'Nothing was released'. Scenario: a large screen (dense OCR text + JPEG) crosses 1 MB → the Activity & Privacy audit trail shows a `cloud_send` of N bytes that never left the machine, and the next step row carries a payload hash 
   FIX: Move the cap check into `observe_and_release` (compute text + base64 image length, compare to `MCP_RESULT_MAX_BYTES`) before `record_cloud_send`/`observed`, returning the cap notice as an error reply; record `byte_count` as the base64 wire length.

[low] Dashboard Agent tab shows tasks left non-terminal by a crash/quit as 'running' forever; no startup reconciliation — src-tauri/src/agent.rs:781
   `build_runtime` constructs a fresh `AgentRuntime` with `driver: None`; nothing scans `tasks` for rows still in `idle`/`running`/`paused` from a previous process. Scenario: Aperture is quit (or crashes) mid-task → on relaunch the Agent tab (Dashboard.tsx:945-1030) lists the task as 'running · 4 steps' indefinitely, `agent_status` reports no task, and the only way to clear it is Purge — which deletes the audit steps the row was supposed to preserve (locked decision 6). `agent_list_tasks` has no wa
   FIX: In `build_runtime` (or `AppState::new`), transition every non-terminal task row to `Failed`/`Cancelled` with `outcome_summary = 'interrupted: Aperture restarted'` via `TaskManager::transition` before the runtime is exposed; alternatively have `agent_list_tasks` mark rows whose id != the current driv

[low] Agent tab 'Purge' deletes a task and its audit steps with no confirmation — ui/src/components/Dashboard.tsx:1007
   `purge(id)` (969-977) calls `agentPurgeTask` on the first click; the button sits directly beside 'Show steps' in the same row head. A mis-click permanently removes the task row and every `task_steps` audit row (payload hashes, actions, results) — the only record of what Claude did and what was sent for that task. `ipc.ts` documents the same requirement for `purgeAll` ('Confirm in the UI BEFORE calling this — it is irreversible') and the Privacy panel honours it; this new irreversible path does n
   FIX: Add the same two-step confirm the Privacy panel uses for Purge All (an 'Are you sure?' state on the row, or `window.confirm`), and visually separate the destructive button from 'Show steps'.

[high] open_windows ships every top-level window title unredacted, including excluded windows and the focused window's raw title — src-tauri/src/agent.rs:681
   agent.rs:681-686 builds `open` from `list_open_windows()` (platform.rs:84-105: every visible titled top-level window, foreground first) as "process — title" and passes it straight into RawObservation; screen-serializer/src/lib.rs:116 copies `observation.open_windows` into the payload with no redact_text pass, while lib.rs:101 redacts only `focused_window.title`. Doc 22 §3.2 specifies `open_windows` as app names only ("VSCode", "Chrome"). Scenario 1: Outlook is focused with title "Inbox - john.do
   FIX: In observe_and_release emit app/process names only (what Doc 22 §3.2 says), or if titles are wanted: run each entry through `redactor.redact_text` in build_step_payload AND drop entries for which `state.exclusions.is_excluded(process, class, title, None)` is Excluded (mirror normalizer's title strip

[high] Screenshot after hard stop / while paused / for a superseded task id: observe_and_release never re-checks the driver before capturing and releasing — src-tauri/src/agent.rs:657
   mcp_step checks terminal/identity at A (line 445-468) and after execute (line 599-604), then sleeps `settle` (300-800 ms, line 607) and calls observe_and_release, which reads `rt.driver` (line 658-667) without checking `is_terminal()`, `state()==Paused`, the stop flag, or `d.task_id()==id`, then captures (673), audits (747) and returns the screen (769). Scenario A (locked decision 5 'hard stop always wins'): Claude's click executes Ok; during the settle sleep or the ~400 ms OCR on the blocking t
   FIX: At the top of observe_and_release (and again immediately before record_cloud_send) take the lock and refuse unless `driver_matches(&rt,id) && !d.is_terminal() && d.state()==TaskState::Running && !rt.stop.load()`; return terminal_reply / a 'paused' notice instead. Consider also checking the stop flag

[high] observe_now drops the URL (url_pattern exclusion rules) whenever the hook-tracked identity is stale instead of refusing like sample_once does — crates/capture/src/sampler.rs:340
   sampler.rs:340 `let url = if fg.identity == identity { fg.url } else { None };` then gates with `url.as_deref()` (343-348) and pulls the frame. `fg` is only updated by the hook drain on ForegroundChanged/TitleChanged (capture/src/lib.rs:296-300), which runs the normalizer's UIA/extension URL resolve first. The agent's own actions cause navigations: Claude clicks a link, Chrome's title changes to 'Accounts — Chase'; after the 300-800 ms settle `capture_time_identity()` already carries the new tit
   FIX: On identity mismatch either fail closed (`return Err(CaptureError::CaptureUnavailable("foreground changed — retry"))` so the loop re-settles and retries) or resolve the URL live for browser processes (the normalizer's extension/UIA address-bar resolve) before gating; never gate a browser frame with 

[high] Multi-line secrets (PEM/OPENSSH private keys) are fully redacted in OCR text but only the BEGIN line is painted in the screenshot — crates/screen-serializer/src/screenshot.rs:81
   Text redaction runs over the whole `ocr.text` (screenshot.rs:93) where the PEM body regex `-----BEGIN ... KEY-----[\s\S]*?-----END ...` (redaction.rs:146-149) spans lines. Image redaction is per OCR line: screenshot.rs:81-92 converts each line separately and image_redaction.rs:103-129 runs `find_spans` on one line's words joined by spaces. A terminal showing `cat ~/.ssh/id_rsa` (or an editor with a .pem open): line 1 '-----BEGIN OPENSSH PRIVATE KEY-----' hits the header-only fallback and is pain
   FIX: Run `find_spans` over the SAME string the text path sees (all kept lines joined with '\n', tracking each word's byte range in that joined string) so a multi-line span paints every word it covers; or, cheaper, when `redact_text(&ocr.text)` reports a secret_key hit whose span touches a line boundary, 

[medium] focused_window.url, last_action.result and the user's clarification answer leave unredacted (v1 redacts the equivalent surfaces) — crates/screen-serializer/src/lib.rs:113
   build_step_payload redacts only ocr_text and focused_window.title (lib.rs:100-101); `url` (113), `last_action` (115) and `prior_steps_summary` (117) are copied verbatim. (a) url: Chrome on 'https://app.example.com/reset?token=eyJhbGciOi...' or '?email=a@b.com' — the JWT/email would be scrubbed by the text rules, and v1 scrubs URLs inside EventTrail JSON via redact_value (redaction.rs:222-226), but here it egresses raw each step. (b) last_action.result is executor text built from screen content: 
   FIX: In build_step_payload run redact_text over url, last_action.target/result, open_windows entries and prior_steps_summary (Claude's own summary is harmless but the 'User answered:' suffix is not); keep the existing idempotence. Alternatively redact the answer in AgentDriver::answer before it is stored

[medium] cloud_send audit row for an agent step is written before the MCP cap check, so a cap refusal leaves a phantom 'sent' row — src-tauri/src/mcp_bridge.rs:227
   agent.rs:747-759 writes the cloud_send row inside mcp_step (and agent.rs:763 stamps the hash on the driver), then mcp_bridge.rs:227-236 checks `reply.text.len()+image_len > MCP_RESULT_MAX_BYTES` and returns 'Nothing was released'. get_context does the cap check BEFORE the audit write (mcp_bridge.rs:309-320 then 322-343). Doc 13 §3 (line 72): 'never a phantom row on a failed send'. Scenario: a tall/square window crop whose noisy JPEG + JSON exceeds 1 MiB → the user's audit feed shows a cloud_send
   FIX: Move the cap check into observe_and_release before record_cloud_send (compute the exact content JSON incl. base64 image first), or have mcp_step return the prepared wire and let mcp_bridge write the audit row after its cap check. Measure the cap on the base64-encoded image.

[medium] wire_sha256 / byte_count in the agent's cloud_send row are computed over bytes that never leave the machine — src-tauri/src/agent.rs:738
   The hash (lib.rs:119-120) is SHA-256 of `serde_json::to_vec(&StepPayload)` in struct field order with screenshot_b64 inline. The released text (agent.rs:738-742) is re-serialized from a `serde_json::Value` — this workspace does not enable `preserve_order` (Cargo.lock: serde_json has no indexmap dep), so keys are emitted in BTreeMap/alphabetical order — with screenshot_b64 replaced by '<attached as image>' (740), plus a trailer appended at 770-772, and the image goes out as a separate base64 bloc
   FIX: Build the exact outgoing content (text string incl. trailer + the base64 image string, or the full MCP content JSON) in observe_and_release, hash THAT for wire_sha256 and count its bytes; keep the StepPayload hash for task_steps.screen_payload_hash if you want both, but document which is which.

[low] Executor exclusion guard never considers the browser URL, so the agent acts on url_pattern-excluded pages — src-tauri/src/agent.rs:73
   ExclusionListProbe::excluded_label calls `is_excluded(process, window_class, title, None)` (agent.rs:73) and UiaExecutor::guard (uia.rs:77-83) only has process/class/title to offer. A rule `url_pattern: .*bank\.example.*` with no title/process component: Claude navigates Chrome to the bank → guard passes → click/type/key actions run on the excluded page (decision #49: 'the executor never acts on an excluded window'). Observation may later pause (if the hook identity matches, see the observe_now 
   FIX: Extend ExclusionProbe with a url parameter; in the probe resolve the live browser URL for the foreground hwnd (the normalizer's extension cache / UIA address bar) and pass it to is_excluded; fail closed for browser processes when no URL can be resolved and url_pattern rules exist.

[low] Image gate silently skips text whose geometry is missing or whose line was quality-dropped, leaving those pixels unpainted — crates/vision-ocr/src/windows_media_ocr.rs:221
   windows_media_ocr.rs:221-229: a failed `line.Words()` yields a line with text but zero words; a failed `word.BoundingRect()`/`Text()` drops that single word. In image_redaction.rs:107-117 the joined string then lacks the text (empty join → no spans; a card split '4111 1111 1111 1111' missing one group → 12 digits → Luhn gate fails → none of the remaining boxes painted), while screenshot.rs:93 still redacts the full line text from `ocr.text` — text says ⟨card#1⟩, JPEG shows the number. Separately
   FIX: In observe_frame, if any kept line has non-empty text and no words (or `redact_text(line.text)` hits but the line's joined words produce no span), fail closed: paint the whole line band (use the min/max of sibling word boxes) or return Err so the step goes text-only. Run the image gate over raw pre-

[low] Executor debug log prints the foreground window title on every action — crates/action-executor/src/uia.rs:300
   uia.rs:300-306 logs `foreground = <title>` and `target` at debug level before each action. A password manager or mail client title ('1Password — Chase login', 'Inbox - john.doe@corp.com') lands in the log file unredacted whenever debug logging is on; the same titles are what doc 13 §4 strips from excluded rows.
   FIX: Log the process name and hwnd only, or pass the title through the Redactor / omit it.


---

[medium] Finding-6 idle paths are unreachable on the primary overlay: the HUD is a permanent .surface-interactive rect, so the JS 10 Hz interval never stops and the Rust poller never drops to 30 Hz — src-tauri/src/hit_test.rs:135
   ui/src/App.tsx:308 mounts <Hud> unconditionally on the primary overlay and ui/src/components/Hud.tsx:94 gives it `surface-interactive`. On first paint useHitTestRects.measure() therefore publishes a non-empty set (the HUD rect) and arms the 100 ms setInterval at ui/src/state/useHitTestRects.ts:71-72; it only clears when a measurement publishes [] (line 68-70), which never happens while the HUD exists (it unmounts only pre-first-run). On the Rust side HitTestState::has_work() (hit_test.rs:132-136) sees that window's non-empty rects and spawn_poller (line 189) keeps POLL_INTERVAL=16 ms forever. 
   FIX: Make the idle decision ignore the permanent ambient chrome: either give the HUD its own always-on rect that does not count as 'work' (e.g. a `data-hit-static` marker the JS publishes separately and has_work() excludes, with the poller reconciling at the idle rate when only static rects exist), or tr

[medium] Cancel while a push Send is in flight is a no-op, and the mismatch/transport-failure restore then resurrects an approved session nobody is displaying (doc 13 §3 zero-residue broken) — src-tauri/src/commands/mod.rs:1607
   preview_send removes the session and the approval hash (mod.rs:1554-1563), releases the lock, then awaits send_with_preview. With the default order and `claude` off PATH, pick_named_transport spawns `claude --version` (cli.rs:92) before answering — hundreds of ms. During that window the panel's Escape handler (ui/src/components/ContextPreviewPanel.tsx:107-110) is NOT gated on `sending` (only the Cancel button is, line 388), and App.tsx:390 calls previewCancel(payload_id) and unmounts the panel; the same happens when another monitor claims the preview (App.tsx:149-154). preview_cancel (mod.rs:1
   FIX: Track in-flight sends in PreviewStore (e.g. `in_flight: HashSet<Uuid>` or a `cancelled: HashSet<Uuid>` tombstone): preview_cancel records the id if it is in flight, and preview_send's Err branch only restores when the id was not cancelled meanwhile (drop backup + hash otherwise). In the panel, gate 

[low] Upgraded installs keep the stored `resolve/main` URL but get the pinned sha256 backfilled, so a future upstream re-upload turns every download into a full-size HashMismatch loop — src-tauri/src/vlm_fetch.rs:113
   main.rs backfill_missing (lines 341-369) is additive-only: for a DB seeded between 2026-08-16 and this tree (the owner's dev machine — the vlm_download block was itself backfilled on 08-19 with the then-current `resolve/main` URLs) it inserts `sha256` beside the existing `url` and never rewrites the url. vlm_fetch::item() then yields url=…/resolve/main/… with sha256=Some(pinned digest) because the key is present (line 113-118; the `url == default_url` guard on line 119 only applies when the key is absent). Today main == 5037fcf so it passes; the moment ggml-org re-uploads the GGUF, each click 
   FIX: In item(), recognise the legacy default (`…/ggml-org/Qwen2.5-VL-3B-Instruct-GGUF/resolve/main/<same file>`) as 'the default URL' and substitute DEFAULT_*_URL (or add a one-off backfill step that rewrites that exact stored value to the pinned one and logs it); keep the sha256 pairing rule as is. Also

[low] Retarget swaps in the core's last-approved payload, silently reverting any item edits the user made after the mismatch — ui/src/components/ContextPreviewPanel.tsx:241
   Send → previewSetApproved syncs items S1 to the session → TransportMismatch banner. User removes an OCR item (removeItem → onChange, panel now shows S2; core still S1, approval dropped only on retarget). User clicks 'Send via Messages API instead' → preview_retarget returns the session payload (S1 + new target) and line 241 does onChange(updated): the removed item reappears on screen and will ship unless the user notices and removes it again. The edit is lost without any message; the 'Now set to … Review and press Send again' copy suggests only the transport changed.
   FIX: Have retarget() merge only the transport: onChange({ ...payload, transport_target: updated.transport_target }) — preview_set_approved re-syncs items from the panel at the next Send anyway — or make preview_retarget accept the current panel payload and sync items exactly like preview_set_approved doe

[low] preview_send's restore-on-error branch also fires for GatewayError::Validation, which is raised AFTER egress and after the cloud_send audit row, so the 'Nothing left the machine' comment is false and a retry re-sends the same bytes — src-tauri/src/commands/mod.rs:1603
   send_with_preview (crates/reasoning-gateway/src/lib.rs:248-289) transmits in step 3, writes the audit row in step 4, and only then validates in step 5; a cloud reply that fails suggestion_validator::validate (malformed/unschema'd JSON from the CLI or API) surfaces as Err(Validation). preview_send's Err arm (mod.rs:1601-1618) treats every non-mismatch error alike: restores the session and the approval hash and returns Err(text) which the panel shows as a retryable failure ('A failed transport leaves the approval retryable'). The user presses Send again → a second egress of identical bytes, a se
   FIX: Match on the error: for GatewayError::Validation (and any post-send failure) do NOT restore the approval — drop the session (or restore it unapproved) and return a distinct message ('Claude replied but the answer could not be validated; nothing to retry') so the panel does not offer a silent re-send

